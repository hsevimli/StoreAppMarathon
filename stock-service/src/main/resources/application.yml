server:
  port: 7071


spring:
  datasource:
    username: postgres
    password: root
    url: 'jdbc:postgresql://localhost:5432/stockDb'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true